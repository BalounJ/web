{% extends "zaklad.twig" %}

{# Prekryti bloku Obsah v predkovi #}
{% block obsah %}
    <article>
        <div class="row">
            <div class="col-sm-12">
                <h1>{{ titulek }}</h1>
                Zde naleznete seznam schválených článků.
            </div>
        </div>
        <h2>Schválené články</h2>
        {% for clanek in schvaleneClanky %}
            <div class="row">
              <!--  <a href="uploads/balounj_generovani-nahodnych-cisel.pdf" class="clanek">
                    <div class="col-sm-4">
                                <h3>{{ clanek.nazev }}</h3>
                                <h4>{{ clanek.autori }}</h4>
                                Hodnocení: {{ clanek.hodnoceni }}
                    </div>
                    <div class="col-sm-8">
                                {{ clanek.abstract }}
                    </div>
                </a>-->

                <form action="clanekOper.php" method="post" target="_blank">
                    <input type="hidden" name="id" value="{{ clanek.id_prispevku }}">
                    <button class="clanek" type="submit" name="action" value="showPDF">
                        <div class="col-sm-4">
                            <h3>{{ clanek.nazev }}</h3>
                            <h4>{{ clanek.autori }}</h4>
                            Hodnocení: {{ clanek.hodnoceni }}
                        </div>
                        <div class="col-sm-8">
                            {{ clanek.abstract }}
                        </div>

                    </button>
                </form>

            </div>
        {% endfor %}

    </article>
{% endblock %}